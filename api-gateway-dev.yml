zuul:
  routes:
    myMapper:
      path: /myTests/**
      serviceId: test
  ignored-patterns:
    - /**/girl
env:
  dev
ribbon:
  #配置请求超时时间,单位s
  ReadTimeout: 1000
  #配置连接超时时间,单位s
  ConnectTimeout: 5000
